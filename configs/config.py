# all three train, eval, data classes MUST be implemented even if any one of them is empty
class model:
    model_name = "distilbert-base-uncased"

class train:
    hi = "this is an example"
    next = "you could also do this"

class eval:
    AND = "you could also do this"

class data:
    pass
